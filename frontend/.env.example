# Адрес API Gateway (все обычные запросы идут сюда)
VITE_API_URL=http://localhost:8080

# Адрес auth-service — нужен ТОЛЬКО для входа через Google.
# В gateway нет маршрута /oauth2/**, поэтому браузер редиректим прямо в auth.
VITE_AUTH_URL=http://localhost:8081
